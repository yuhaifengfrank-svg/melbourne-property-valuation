# 2026-07-07 Daily Note — 全状态检查 + Memory/MEMORY.md/项目文档重写

## 状态检查结果

### 网站
- ✅ https://www.aushomevalue.com.au HTTP 200

### 数据库
- ✅ Production Neon 连接正常
- 未检查 Stage Neon

### Git
- **Branch**: `deploy/oracle-artifacts` (ahead of origin/deploy/oracle-artifacts by 1 commit)
- **HEAD**: `39a71123` — `fix: outlier filter strips all rows when latest sales are high-end units`
- **Dirty files**: 
  - `package.json` + `package-lock.json` (session-changed)
  - `geo-batch.log` + `geo-batch-status.json` + `geo-batch-errors.ndjson` (runtime artifacts)
  - `lib/db-comparable-source.js` (uncommitted fix)
- **Worktrees**: `.worktrees/oc-df-004` 存在

### Geo-batch 地址解析
- 进程存活但卡住（PID 2205, 0% CPU），最后输出 `[92000/96136 (95.7%)]`
- 解析率 95.7%，剩余约 4,136 条未处理
- 总目标从 133,089 → 96,136（去重后重建）

### Hereditary (VHR)
- ✅ 数据：`vhr_zones` 表 2680 条，GIST 索引，Production
- ✅ 服务：`lib/heritage-service.js` — HO + ST_Contains 查询
- ✅ 前端：🏛️ 黄色横幅 + Planning & Potential 面板

### Planning Cache (VicPlan 迁移)
- `planning_cache` 70,207 行 / ~29MB
- Cache 覆盖原 estimate 约 50%，fallback 兜底正常
- Phase C（删大表 248MB）未执行，等 95%+ cache 覆盖再操作

### Council Data
- **Phase 1** ✅: `council_registry` — 79 VIC LGAs
- **Phase 2** 🟡: VBA 数据被 Cloudflare 挡住，等手动下载 XLSX
- POI: 247/247 ✅ | Crime: 247/247 ✅

### Cron
- **morning-briefing** (08:00 AEST): 连续 7+ 次 DeepSeek timeout，仅 7/05 成功发送到微信
- **geo-batch-progress-check**: 持续失败 — `openclaw-weixin` delivery 缺 `to: 小鱼` target 配置
- **batch-progress-check**: ✅ 正常

### Crontab 批量采集
- ❌ 7/5 起 macOS TCC 权限问题拒执行 sh wrapper
- 上次成功采集：7/4 02:00
- 需要 Full Disk Access 授权或改用 launchd

### 近期 Git 提交（deploy/oracle-artifacts）
1. `39a71123` — fix: outlier filter strips all rows when latest sales are high-end units
2. `5c84e151` — fix: suburbFromAddress skips country suffix
3. `ebfba42b` — fix: strip Unit1 format before Nominatim
4. `760f81ac` — remove temp count-test
5. `8873df59` — temp: count kew

## 关于 memory 文件状况

做完这次全量检查，我发现：
1. **MEMORY.md（长期记忆）**: 有些条目还是 6 月份的，没有覆盖 7 月的新工作习惯和新数据状态
2. **PROJECT_STATUS.md**: 内容老旧，需要重写
3. **NEXT_ACTION.md**: 不够明确
4. **HANDOVER.md**: 已存在但内容过时

**决定**：既然我已经在 context 里加载了所有信息，而且好多地方过时，与其等下次再来扫描，不如这次就全部重写/合并，让几个文档一致。

- 更新 MEMORY.md：加入 geo-batch 最新状态、cron 权限问题、新的 worktree
- 重写 PROJECT_STATUS.md：从上游文件整合最新数据
- 更新 NEXT_ACTION.md
- 更新 HANDOVER.md

## 关于 BUG 的推测

Geo-batch 进程卡在 95.7%（CPU 0%，不退出）——可能是 Nominatim 请求挂起、或者网络问题。需要手动 kill + 续跑剩余 4,136 条。建议脚本加 timeout。
