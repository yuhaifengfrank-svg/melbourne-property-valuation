# 2026-07-03 Daily Note

## Daily Batch Cron 修复

### 问题
- OpenClaw cron (isolated agentTurn) 凌晨 DeepSeek API 全失败（LLM request failed）
- 3 个 cron job 连续失败 3-5 次，`collection_state.last_batch` 一直为 0
- 两套 cron 打架：OpenClaw cron (02:00+04:00) + crontab (03:00 6 条按星期几分批次)
- crontab 旧 batch 只有 6 天 6 个 batch（每天 1 个），原设计应该是 12 batch/6 天

### 修复方案 A：迁移到 crontab，保留 2 slots/天
1. **删除旧 crontab batch**：6 条 `0 3 * * 1-6 cron-batch.sh 1-6`
2. **新增 2 个 cron wrapper 脚本**（从 .env 读 DATABASE_URL，export NODE_PATH）
   - `cron-batch-slot1.sh` → 02:00
   - `cron-batch-slot2.sh` → 04:00
   - 均调用 `daily-batch-collection.mjs` 无参数（auto-advance 模式）
3. **coastal catchup 也做成独立 .sh wrapper**：`cron-coastal.sh` → 05:30
4. **禁用 OpenClaw cron**：
   - `daily-batch-slot1` (f2ae9698) → disabled
   - `daily-batch-slot2` (250bdd93) → disabled
   - `coastal-catchup` (0935b6ce) → disabled
5. **cron-batch.sh**（旧 6 天 wrapper）不再使用但保留

### collection_state 状态
- `last_batch: 0, last_run_date: null` — 凌晨批次未执行，正好从头开始
- auto-advance 逻辑验证：第一次 slot1 无参数 → `!st.last_run_date` → batchNum=1
- slot1 跑完更新 `last_batch=1` → slot2 解析为 batch 2

### 关键文件
| 文件 | 说明 |
|------|------|
| `cron-batch-slot1.sh` | 02:00 batch slot |
| `cron-batch-slot2.sh` | 04:00 batch slot |
| `cron-coastal.sh` | 05:30 coastal catchup |
| `cron-batch.sh` | 旧 wrapper（已废弃但保留） |

### 剩余问题
- batch 脚本执行中电脑可能睡眠导致 cron 不触发（macOS 睡眠时 cron 不自动唤醒来跑任务）→ 需要 Talkback/WakeOnLAN 或让电脑不 sleep
- `cron-macro.sh` 文件不存在，crontab 中的 macro 行已删除（历史遗留，可能 macro 数据采集已不再使用）
